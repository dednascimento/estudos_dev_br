Algoritmo "exercicio_tabuada"

Var

   N, i, X: inteiro

Inicio

      escreva("Deseja a tabuada para qual valor? ")
      leia(N)
      
      X <- N
      
      escreval()
      
      para i de 1 ate 10 faca

        escreval(X, " x ", i:2, " = ", N)
        N <- N + X

      fimpara


Fimalgoritmo