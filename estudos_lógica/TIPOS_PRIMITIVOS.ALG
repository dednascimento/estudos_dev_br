Algoritmo "tipos_primitivos"


Var
   a: real
   b: inteiro

Inicio
      a <- 5.0
      b <- Int(a)
      
      escreva(a, b)

      // No caso mesmo que o valor 'a' decimal esteja com 0 após a virgular ele não irá permirtir
Fimalgoritmo